{{ form_start(form) }}
    <div style="padding:10px; " >  {{ form_row(form.text) }}</div>
    <div style="padding:10px" >  {{ form_row(form.problem_id) }}</div>
    <button style="width:7%;margin-left:10px;margin-top:20px" class="btn btn-primary">{{ button_label|default('Save') }}</button>
    <a style="width:9%; height: 13%;margin-left:10px;margin-top:20px;size:10px"class="btn btn-dark" href="{{ path('answer_index') }}">back to list</a>
{{ form_end(form) }}
